<% if @assigned_project.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@assigned_project.errors.count, "error") %>.
    </div>
    <ul>
    <% @assigned_project.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>